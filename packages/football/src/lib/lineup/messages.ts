import { defineMessages } from 'react-intl';

export const lineupMessages = defineMessages({
  prizePoolIncentive: {
    id: 'Lineup.prizePoolIncentive',
    defaultMessage: '10x more users rewarded',
  },
});
